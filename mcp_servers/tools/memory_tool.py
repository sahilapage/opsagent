# Phase 5 stub — pgvector long-term memory coming
def get_memory(user_id: str) -> str:
    return f"No memory stored yet for user: {user_id}"

def store_memory(user_id: str, content: str) -> str:
    return f"Memory storage coming in Phase 5 (pgvector)."
